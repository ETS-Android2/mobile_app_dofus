<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ThemecolorPrimary</item>
        <item name="colorPrimaryDark">@color/ThemecolorPrimaryDark</item>
        <item name="colorAccent">@color/ThemecolorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.ActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dark application theme. -->
    <style name="DarkAppTheme" parent="ThemeOverlay.AppCompat.Dark">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ThemecolorAccent</item>
        <item name="colorPrimaryDark">@color/ThemecolorPrimaryDark</item>
        <item name="colorAccent">@color/ThemecolorAccent</item>
        <item name="android:colorBackground">@color/softBG</item>
        <item name="android:textColorPrimary">@color/TextWhite</item>
        <item name="android:textColorSecondary">@color/TextWhite</item>
        <item name="editTextColor">@color/TextWhite</item>
        <item name="android:textColorTertiary">@color/TextWhite</item>
    </style>

    <style name="DarkAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DarkAppTheme.ActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DarkAppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="DarkAppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

</resources>
